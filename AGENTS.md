# Agent 工作约定

## 项目事实

这是 GitHub 个人主页仓库，主要内容是英文 `README.md` 和 `images/` 中的 SVG。README 提及的 ArkUI、Cloudflare 等属于个人经历和外部项目，不代表本仓库的技术栈。

当前没有构建配置、可执行测试或 CI 工作流；不要根据空的 `scripts/`、`tests/`、`docs/`、`data/` 目录推断功能。项目变化后，以实际文件为准并同步修正本说明。

## 执行任务

- 以用户要求的结果为完成标准。在系统与开发者规则允许的范围内，用户明确指令优先于本文件的项目约定和 skill 指南。
- 对范围内、可逆的工作直接推进到完成与验证；一般实现细节作合理选择，影响结果的假设简要说明。只有缺失信息会显著影响正确性、范围或授权时才询问，并继续不依赖答案的工作。已有授权无需重复确认。
- 先检查相关文件和工作区改动，保留用户已有修改。优先用 `rg` 定位所需内容；独立的读取和检查可并行。可独立分工且能节省时间或提高质量时使用子代理，明确职责并检查结果。
- 只修改完成任务所需的内容。相邻的 Blog 和 README 链接的其他仓库是独立项目；涉及它们的修改需有对应任务范围。
- 个人经历、职责、项目状态和联系方式以用户提供的信息与现有资料为依据，不补写未经证实的事实。默认保留 README 的英文正文和 Markdown/HTML 风格；沟通跟随用户语言。

## Skills 与指令维护

- 当前没有项目级 skill。根据实际任务选择可用技能，按需读取 `SKILL.md` 及相关引用；不因 README 出现技术名称而加载该技术的开发流程。
- 新增项目级 skill 时放在 `.agents/skills/<name>/SKILL.md`，提供准确的 `name` 和 `description`，只沉淀可复用且不显然的项目知识。一次性改动不需要新建 skill。
- 本文件保留项目事实和通用约定，专项流程放入对应 skill；避免重复规则、失效路径、固定模型版本和无条件全量测试。删除旧 skill 或资源前核实用途及引用，不以暂未使用为由判定过时。
- 若 skill 导致暂停、额外确认或偏离用户目标，指出并链接实际读取的 `SKILL.md`，引用相关条款，说明这是明确要求还是解释判断。

## 验证与交付

- 文案或指令改动：审阅 diff、事实和引用路径，运行 `git diff --check`。新增未跟踪文件也要检查内容和空白；不为纯文案改动引入测试框架。
- 图片或排版改动：检查受影响的本地引用；修改 SVG 时验证 XML 可解析并查看渲染效果。新增或更改外部链接时按任务需要核实目标。
- 有对应行为改动时执行相关检查；通过后即可交付，只有新变更、失败或未解决疑点才扩大或重复验证。
- 最终简要说明改了什么、如何验证，以及实际未完成或未验证的部分。区分已执行的检查和建议，不把不存在的测试报告为通过。

维护参考：[OpenAI 模型指南](https://developers.openai.com/api/docs/guides/latest-model)、[AGENTS.md](https://learn.chatgpt.com/docs/agent-configuration/agents-md)、[Skills](https://learn.chatgpt.com/docs/build-skills)。仅在维护这些指令或需要核实相关规则时查阅。
